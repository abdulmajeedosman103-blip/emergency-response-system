// frontend/src/components/SosBadge.jsx
// Small red indicator shown for incidents submitted via the SOS flow.
export default function SosBadge() {
  return (
    <span className="inline-flex rounded bg-red-600 px-1.5 py-0.5 text-[10px] font-bold uppercase tracking-wide text-white">
      SOS
    </span>
  );
}